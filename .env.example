# RoboTable Environment Variables
# Copy this file to .env and fill in your values

# ==============================================
# DATABASE CONNECTION
# ==============================================

# Option 1: Supabase PostgreSQL (Recommended for Production)
# Get this from: Supabase Dashboard → Project Settings → Database → Connection String
# Format: pg://db.[project-ref].supabase.co:5432?u=postgres&p=[password]&d=postgres
NC_DB=pg://db.your-project-ref.supabase.co:5432?u=postgres&p=your-password&d=postgres

# Option 2: Local PostgreSQL
# NC_DB=pg://localhost:5432?u=postgres&p=password&d=robotable

# Option 3: MySQL
# NC_DB=mysql2://localhost:3306?u=root&p=password&d=robotable

# Option 4: SQLite (Default - no config needed for local testing)
# Leave NC_DB unset to use SQLite with Docker volume

# ==============================================
# AUTHENTICATION
# ==============================================

# JWT Secret for authentication (generate random secure string)
# Generate with: openssl rand -hex 32
NC_AUTH_JWT_SECRET=your-random-secure-jwt-secret-here-min-32-characters

# ==============================================
# PUBLIC ACCESS
# ==============================================

# Public URL where RoboTable is accessible
# Local: http://localhost:8080
# Render: https://robotable.onrender.com
# Custom: https://robotable.yourcompany.com
NC_PUBLIC_URL=http://localhost:8080

# ==============================================
# OPTIONAL SETTINGS
# ==============================================

# Disable telemetry (recommended)
NC_DISABLE_TELE=true

# Port (default: 8080)
PORT=8080

# Admin user (optional - create via UI instead)
# NC_ADMIN_EMAIL=admin@yourcompany.com
# NC_ADMIN_PASSWORD=SecurePassword123!

# Database connection pool (optional - defaults are fine)
# NC_MIN_DB_POOL_SIZE=2
# NC_MAX_DB_POOL_SIZE=10

# ==============================================
# NOTES
# ==============================================
#
# 1. Never commit .env file to git (already in .gitignore)
# 2. For Render deployment, set these in Render dashboard instead
# 3. Generate strong JWT secret: https://www.random.org/strings/
# 4. Supabase connection string includes password - keep secure!
